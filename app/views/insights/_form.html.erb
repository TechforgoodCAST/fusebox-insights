<%= bootstrap_form_for([@project, insight]) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :description %>
  <%= f.submit "Save", data: { disable_with: 'Updating...' }, class: 'btn btn-info' %>
<% end %>
