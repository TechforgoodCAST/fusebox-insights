<%= simple_form_for([@project, insight]) do |f| %>
  <%= f.input :title %>
  <%= f.input :description, as: :trix_editor %>
  <%= f.button :submit, data: { disable_with: 'Updating...' }, class: 'btn btn-info' %>
<% end %>
