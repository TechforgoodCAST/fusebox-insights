<div class="flex mb30 justify-between items-center">
  <h1 class="fs20 bold">Groups</h1>
  <%= link_to 'New Group', new_group_path, class: 'medium' %>
</div>

<div class="flow flow20">
  <% @groups.each do |group| %>
    <%= render partial: 'card', locals: { edit: false, group: group } %>
  <% end %>
</div>

<%= paginate @groups %>

