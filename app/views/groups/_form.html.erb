<%= bootstrap_form_for([@project, group]) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :description %>
  <%= f.text_area :summary, as: :trix_editor %>
  <%= f.submit "Save group", data: { disable_with: 'Updating...' }, class: 'btn btn-info' %>
<% end %>
