<%= render partial: 'projects/subnav' %>

<div class="max-w-3xl mx-auto px-4 my-8">
  <p class="mb-6"><%= link_to '‹ Back', project_iteration_path(@project, @iteration), class: 'underline' %></p>

  <div class="card">
    <h1 class="text-2xl font-bold mb-6">Edit iteration</h1>
    <%= render 'form' %>
  </div>
</div>