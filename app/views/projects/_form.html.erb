<%= bootstrap_form_for(project) do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>
  <%= f.check_box :is_private %>
  <div class="actions pt-4">
    <%= f.submit "Save project", data: { disable_with: 'Updating...' }, class: 'btn btn-info' %>
  </div>
<% end %>
