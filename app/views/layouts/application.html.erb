<!DOCTYPE html>
<html>
  <head>
    <title>FuseboxInsights</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="mx15 my15 text-right grey">
      Signed in as <%= current_user&.username %> ·
      <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'medium grey' %>
    </header>

    <main>
      <div class="flex flex-wrap">
        <nav class="col1 md">
          <%= link_to 'In focus', in_focus_path, class: current_controller?('foci') %>
          <%= link_to 'Unknowns', unknowns_path, class: current_controller?('unknowns') %>
          <%= link_to 'Insights', insights_path, class: current_controller?('insights') %>
        </nav>

        <div class="col3 mx15">
          <p id="notice"><%= notice %></p>
          <p id="alert"><%= alert %></p>

          <%= yield %>
        </div>
      </div>
    </main>

    <footer class="mx15 my30 fs14 grey text-center">
      This is a prototype, <%= mail_to 'suraj@wearecast.org.uk', 'your feedback will help us improve this service', subject: 'Feedback fusebox-insights', class: 'grey medium' %>.
    </footer>
  </body>
</html>
