<%= bootstrap_form_for(@programme) do |form| %>
 
  <% if @programme.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@programme.errors.count, "error") %> prohibited
        this programme from being saved:
      </h2>
      <ul>
        <% @programme.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <%= form.text_field :title %>
  <%= form.text_area :description %>
  <%= form.submit %>
 
<% end %>